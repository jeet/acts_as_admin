<table style="width:300px">
  <tr>
    <% width = get_dt_length(@columns)%>
    <% @columns.each do |col| %>
     <td style="width:<%=width-%>%;">
       <%= col %>
     </td>
    <% end %>
  </tr>
  <% @objects.each do |object| %>
   <tr>
     <% @columns.each do |col|  %>
     <td style="width:<%=width-%>%;">
        <%= object.send(col.to_sym) %>
     </td>
         <%end %>
   </tr>


  <% end %>
</table>
<%= will_paginate @objects %>

<%= link_to "admin home",administrators_url %>