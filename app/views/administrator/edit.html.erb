<%= render :partial => "breadcrumb", :locals => {:action => "edit",:object => @object}%>


<% semantic_form_for  @object, :url=> update_record_admin_path(@mod.to_s,@object.id) do |form| %>

    <%= form.inputs %>


    <% form.buttons do %>
        <%= form.commit_button %>
    <% end %>

<% end %>
<div>

 <%= link_to @mod.to_s, administrator_index_path(@mod.to_s) %> ||
  <%= link_to "admin home",administrators_url %>
</div>
